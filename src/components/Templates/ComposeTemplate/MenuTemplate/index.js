import { connect } from 'react-redux';

import MenuTemplate from './MenuTemplate';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(null, null)(MenuTemplate)