import React from 'react';

import { useStyles } from './style';
// import MedMapTabPanel from './MedMapTabPanel';

const ApplicationsPage = props => {
  const classes = useStyles();

  return (
    <main>
      <div>ApplicationsPage</div>
    </main>
  )
};

export default ApplicationsPage;