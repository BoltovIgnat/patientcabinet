import {makeStyles} from "@material-ui/styles";
import { WHITE, BLUE, GRAY } from '../../../../Templates/MedMap/variables';

const useStyles = makeStyles(theme => ({
  root: {
    width: '100%',
    paddingTop: 64,
  },
}));

export default useStyles;