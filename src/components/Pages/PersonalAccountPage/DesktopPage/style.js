import {makeStyles} from "@material-ui/styles";
import {BLACK, BLUE, GRAY, WHITE} from "../../../Templates/MedMap/variables";

const useStyles = makeStyles(theme => ({
  root: {
    width: '100%',
    paddingTop: 64,
  },
}));

export default useStyles;